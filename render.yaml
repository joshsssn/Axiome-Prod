services:
  # --------------------------------------------------------------------------------
  # BACKEND SERVICE (FastAPI)
  # --------------------------------------------------------------------------------
  - type: web
    name: portfolio-api
    runtime: python
    region: oregon # or frankfurt, etc.
    plan: free # or starter
    
    # Point to the API directory
    rootDir: API
    
    # Build & Start commands
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port 10000
    
    # Environment Variables
    envVars:
      - key: PORT
        value: 10000
      - key: DATABASE_URL
        fromDatabase:
          name: portfolio-db
          property: connectionString
      - key: REDIS_URL
        fromService:
          type: redis
          name: portfolio-redis
          property: connectionString
      - key: SECRET_KEY
        generateValue: true
      - key: POSTGRES_USER
        fromDatabase:
          name: portfolio-db
          property: user
      - key: POSTGRES_PASSWORD
        fromDatabase:
          name: portfolio-db
          property: password
      - key: POSTGRES_DB
        fromDatabase:
          name: portfolio-db
          property: database
      - key: POSTGRES_SERVER
        fromDatabase:
          name: portfolio-db
          property: host
      - key: POSTGRES_PORT
        fromDatabase:
          name: portfolio-db
          property: port

  # --------------------------------------------------------------------------------
  # FRONTEND SERVICE (React + Nginx)
  # --------------------------------------------------------------------------------
  - type: web
    name: portfolio-ui
    runtime: docker
    region: oregon
    plan: free
    
    # Point to the UI directory for Docker context
    rootDir: UI
    dockerfilePath: Dockerfile
    
    # Inject the Backend URL into Nginx
    envVars:
      - key: BACKEND_URL
        fromService:
          type: web
          name: portfolio-api
          envVarKey: RENDER_EXTERNAL_URL

  # --------------------------------------------------------------------------------
  # REDIS SERVICE
  # --------------------------------------------------------------------------------
  - type: redis
    name: portfolio-redis
    region: oregon
    plan: free # check availability
    ipAllowList: [] # internal only

databases:
  - name: portfolio-db
    databaseName: portfolio_db
    user: axiome_user
    plan: free # 90 days limit on free tier, upgrade for persistence
